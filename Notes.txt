Have python installed

Navigate to project root directory

Create a virtual environment by pasting this in terminal:
python -m venv venv

Activate the virtual environment with this terminal command:
venv\Scripts\activate

Install streamlit in the virtual environemnt with this command:
pip install streamlit

Run project in virtual environment with:
streamlit run app.py

RAG = Retrieval-Augmented Generation.
It's a pattern that lets an LLM look things up instead of making things up.

Install python-docx and pdfplumber IN THE VIRTUAL ENVIRONMENT
    pip install pdfplumber
    pip install python-docx

Install free embedding model: sentence-transformers (HuggingFace)
pip install sentence-transformers

Install numpy just in case it isn't already
pip install numpy


For OpenAI only:
    Install dotenv for secure api key storing
    pip install python-dotenv

    pip install openai

pip install groq

Mechanics / Power-Ups
    What does the EMP Pulse power-up do?
    How much damage does a Warrior’s Rage ability deal?
    What effect does the Shield Boost have?

Enemies / Weaknesses
    Which enemy is weak to fire spells?
    How much health does the Dragon have?
    What are the Orc’s attack patterns?

Quests / Objectives
    What is the first step in the “The Lost Artifact” quest?
    What level is required to start the “Lost Artifact” quest?
    What rewards do players get for completing the quest?

Level / Environment
    What hazards are in the Castle Siege level?
    How many waves of enemies must players survive?
    What does the Reinforced Shield power-up do?

General / Narrative
    How does the player’s choice affect endings in Space Raiders?
    What are the special abilities available to the Mage class?
    Which enemy requires coordinated fleet attacks?